name: Update ceshi Rules

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule: 
    - cron:  '0 2 */3 * *'

jobs:
  build:
    name: Generate ceshi Rules
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.x
      uses: actions/setup-go@v2
      with:
        go-version: ^1.14
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Get dependencies
      run: |
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi

    - name: Create ceshi folder
      run: mkdir -p ceshi

    - name: Obtain AD lists
      run: |
        curl -LR -o ceshi/1.txt "https://raw.githubusercontent.com/fmz200/wool_scripts/main/QuantumultX/filter/fenliu.list"
        curl -LR -o ceshi/8.txt "https://cdn.jsdelivr.net/gh/FaNGbABa/xiaobaishihundan@main/RuleSet/AdReject/Ad_Normal.yaml"
        sed -i 's/reject//g' ceshi/1.txt
        sed -i 's/[[:space:]]//g' ceshi/1.txt
        sed -i 's/HOST/DOMAIN/g' ceshi/1.txt
        
    - name: Merge lists and remove duplicates
      run: |
        awk 'FNR==1{print ""}{print}' ceshi/*.txt > ceshi/02merge.txt
        awk '!seen[$0]++' ceshi/02merge.txt > ceshi/ceshi.yaml
        sed -i '1d' ceshi/ceshi.yaml
        sed -i '/#/d' ceshi/ceshi.yaml

    - name: Push artifacts to woshiyishi branch
      run: |
        git config --local user.email "523537295@qq.com"
        git config --local user.name "FaNGbABa"
        git fetch
        git checkout woshiyishi
        git checkout --orphan woshiyishi-orphan
        git rm -rf .
        cp -rf ceshi/ceshi.yaml ./
        git add ceshi.yaml
        git commit -am "Updated at $(date)"
        git branch -D woshiyishi
        git branch -m woshiyishi

    - name: GitHub Push
      uses: ad-m/github-push-action@v0.6.0
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: woshiyishi
        force: true
